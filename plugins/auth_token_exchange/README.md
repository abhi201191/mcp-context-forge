# TokenExchange for Context Forge MCP Gateway

Plugin to exchange token.


## Installation

1. Copy .env.example .env
2. Enable plugins in `.env`
3. Add the plugin configuration to `plugins/config.yaml`:
